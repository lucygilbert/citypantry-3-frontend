angular.module('cp.controllers.customer', []);

angular.module('cp.controllers.customer').controller('CustomerChangePasswordController',
        function($scope) {
    // @todo - change page title
    // @todo - require logged in
    // @todo - function to save new password.
});
